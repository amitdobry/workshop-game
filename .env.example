# Copy this file to server/.env and fill in the real values.
# Never commit server/.env. Never paste a real connection string into chat or code.

# MongoDB Atlas connection string for the workshop cluster.
MONGODB_URI=

# Name of the database the game will use (later — right now it uses nothing).
MONGODB_DB_NAME=workshop_game

# Port the server listens on.
PORT=4000
